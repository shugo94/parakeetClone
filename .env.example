# Interview AI - Environment Config
# API keys are stored securely in the app Settings panel (click ⚙ in the overlay)
# You do NOT need to set these here. Use the in-app Settings instead.

# Optional: override for development only
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
